<h1>Editing client</h1>

<% form_for(@client) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f}%>
  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @client %> |
<%= link_to 'Back', clients_path %>
